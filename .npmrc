; 默认从 npmjs 安装，避免本地未配置 GitHub Packages token 时出现 401
@nsnanocat:registry=https://registry.npmjs.org

; 如需改用 GitHub Packages（例如内部测试），请在本机 ~/.npmrc 里配置：
; @nsnanocat:registry=https://npm.pkg.github.com
; //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
